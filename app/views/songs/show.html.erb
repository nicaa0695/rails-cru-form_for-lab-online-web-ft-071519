<h1>My Song</h1>
 <h2><%= @song.name %></h2>

      <h3><%= @genre.name %>
     <%= link_to @genre.name, genre_path(@genre) %>

      <h3><%= @artist.name %>
     <%= link_to @artist.name, artist_path(@artist) %>